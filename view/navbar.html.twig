<nav>
    <div class="nav-wrapper teal accent-4 ">
        <!-- Btn opciones de side bar -->
        <a href="#" data-activates="slide-out" class="button-collapse">
            <span>
                <span>
                    <i class="fa fa-bars" aria-hidden="true"></i>
                    <strong>{{ titulo }}</strong>
                </span>
            </span>
        </a>
        <div class="false-navbar">
            <!-- Logo -->
            <a href="index.php" target="_self" class="brand-logo hide-on-med-and-down hospital">
                <img class="responsive-img" id="logo" src="/public/imgs/logo3.png" alt="Logo del sitio."/>
                <small>
                    <strong>{{ titulo }}</strong>
                </small>
            </a>
            <!-- Barra principal con links alineados a la izq excepto el logo -->
            <ul class="right hide-on-med-and-down">

                {% if isLogged %}
                <!-- Dropdown user -->
                <ul id="dropdownUser2" class="dropdown-content">
                    <li>
                        <a href="?controller=login&action=logout" target="_self"><span class="fa fa-sign-in" aria-hidden="true"></span> Log out</a>
                    <br>
                    </li>
                </ul>
                <!-- Dropdown User Trigger -->
                <li>
                    <a class="dropdown-button" href="#!" data-activates="dropdownUser2">{{user}} &#9660;</a>
                </li> 
                <li class="">
                    <a href="?controller=login&action=logout" target="_self">Log out</a>
                </li>
                {% if (paciente_index == true)  %}
                         <li class="">
                            <a href="?controller=Pacientes&action=index" target="_self">Pacientes</a>
                        </li>
                {% endif %} 

                <!-- Dropdown Structure -->
                <ul id="dropdown1" class="dropdown-content">
                    <li>
                        <a href="?controller=Users&action=index" target="_self">Usuarios</a>
                    </li>
                    <li>
                        <a href="#!">Roles</a>
                    </li>
                    <li>
                        <a href="#!">Permisos</a>
                    </li>
                    <li class="divider"></li>
                    <li>
                        <a href="?controller=Config&action=admin">Configuracion</a>
                    </li>
                </ul>
                <!-- Dropdown Trigger -->
                {% if (paciente_new == false)  %}
                         <li>
                            <a class="dropdown-button" href="#!" data-activates="dropdown1">Administracion &#9660;</a>
                        </li>  
                {% endif %} 
              

                {% else %}
                <li class="">
                    <a href="?controller=login&action=index" target="_self">Log in</a>
                </li>
                {% endif %} 

            </ul>
        </div>
        <!-- Side bar -->
        <ul class="side-nav" id="slide-out">

            <!-- Links -->
            <li>
                <a href="index.php">
                    <span class="fa fa-home" aria-hidden="true"></span> Inicio</a>
            </li>
            {% if isLogged %}
            <li>
                
                <a href="?controller=login&action=logout">
                    <span class="fa fa-sign-in" aria-hidden="true"></span> Log out</a>
            </li>
             {% if (paciente_index == true)  %}
                    <li class="">
                     <a href="?controller=Pacientes&action=index" target="_self"> <span class="fa fa-users" aria-hidden="true"></span>Pacientes</a>
                    </li>
            {% endif %} 
        
            <!-- Dropdown Structure -->
            <ul id="dropdown2" class="dropdown-content">
                <li>
                    <a href="?controller=Users&action=index">Usuarios</a>
                </li>
                <li>
                    <a href="#!">Roles</a>
                </li>
                <li>
                    <a href="#!">Permisos</a>
                </li>
                <li class="divider"></li>
                <li>
                    <a href="#!">Configuracion</a>
                </li>
            </ul>
            <!-- Dropdown Trigger -->
            {% if (paciente_new == false)  %}
                         <li>
                            <a class="dropdown-button" href="#!" data-activates="dropdown1">Administracion &#9660;</a>
                        </li>  
                {% endif %} 
            
            {% else %}
            <li>
                <a href="?controller=login&action=index">
                    <span class="fa fa-sign-in" aria-hidden="true"></span> Log in</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>